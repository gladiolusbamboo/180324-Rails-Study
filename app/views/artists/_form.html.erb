<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :listener_id %>
    <%= form.text_field :listener_id, id: :artist_listener_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :birth %>
    <%= form.date_select :birth, id: :artist_birth %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_area :url, id: :artist_url %>
  </div>

  <div class="field">
    <%= form.label :ctype %>
    <%= form.text_field :ctype, id: :artist_ctype %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo, id: :artist_photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
